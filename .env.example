# ============================================================
# CHECADOR DEPI — frontend
# Copia este archivo a .env y completa los valores
# NUNCA commitees .env (está en .gitignore)
# ============================================================

# URL base del backend admin — sin slash final
VITE_ADMIN_URL=http://localhost:8080
# URL base del backend client — sin slash final
VITE_CLIENT_URL=http://localhost:8081
